apiVersion: v1
kind: Service
metadata:
  name: kelascendikia-service
  namespace: kelascendikia
spec:
  type: NodePort
  selector:
    app: kelascendikia
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 31000
