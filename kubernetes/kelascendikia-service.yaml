apiVersion: v1
kind: Service
metadata:
  name: KelasCendikia-service
  namespace: kelascendikia
spec:
  type: NodePort
  selector:
    app: KelasCendikia
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000
